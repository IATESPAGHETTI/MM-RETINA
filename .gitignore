# Large model weights / dataset archives — not suitable for git
*.zip
*.h5
*.keras
gamma_manifest.json
audit_report.json

# Actual GAMMA dataset clone(s) (license-restricted, huge, own git/LFS remote)
Adv_prj4/GAMMA/
/GAMMA/
dataset/GAMMA/
dataset/GAMMA_noxet/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Trained model checkpoints and raw run logs — large/derived, not source.
# Real results (metrics.json/confusion_matrix.csv/predictions.csv) under
# results/ ARE tracked once they exist, since those are small text summaries
# of an actual run, not weights.
training/checkpoints/
*.log

# Node / Next.js (website/)
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env*
.vercel/

# OS
.DS_Store
Thumbs.db
